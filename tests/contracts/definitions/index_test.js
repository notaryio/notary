describe('load() in the contracts definition helper', () => {
  it('returns parsed(YML-to-JS) object for valid input', async () => {});
  it('throws an error if the contracts.yml file is missing', async () => {});
  it('throws an error if the contracts.yml schema is invalid', async () => {});
  it('throws an error if any of the contracts uses an invalid integration type', async () => {});
});
