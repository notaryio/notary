describe('findAllWithinWorkspace() in contracts repository', () => {
  it('returns array of contracts (both expectations & promises) for valid input', async () => {});
  it('returns array of contracts (only expectations) for valid input', async () => {});
  it('returns array of contracts (only promises) for valid input', async () => {});
  it("throws an error if the contracts.yml couldn't be loaded", async () => {});
  it('throws an error if the contracts.yml schema is invalid', async () => {});
  it('throws an error if one of the contracts has an invalid integration type', async () => {});
});
