modules:
  schema:
    prototypes:
        - name: trackingEvent
          schema:
            type: object
            properties:
              name:
                type: string
              category:
                type: string
                enum: ["product-interactions", "conversion-events", "personalisation"]
              description:
                type: string
              fieldsSchema:
                type: object
            required:
              - name
              - category
              - description
              - fieldsSchema
projects:
  - repo: barebones
  - repo: good-consumer

  - repo: good-rest-provider
  - repo: good-rest-consumer
  - repo: broken-rest-provider
  - repo: broken-rest-consumer

  - repo: good-localstorage-provider
  # - repo: good-localstorage-consumer
  - repo: broken-localstorage-provider
  # - repo: broken-localstorage-consumer

  - repo: good-schema-provider
  - repo: good-schema-consumer
  - repo: broken-schema-provider